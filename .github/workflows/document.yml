on:
  issue_comment:
    types: [created]
name: Document
jobs:
  document:
    name: document
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: jimhester/setup-r@master
      - name: Install dependencies
        script: Rscript -e 'install.packages("remotes")' -e 'remotes::install_deps(dependencies = TRUE)'
      - name: Document
        uses: jimhester/document-v2@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
